import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    padding: 10,
    borderRadius: 5,
    marginVertical: 16,
  },
  textContainer: {
    textAlign: 'center',
  },
});
